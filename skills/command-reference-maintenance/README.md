# Command Reference Maintenance

Generate schema-aligned Markdown, JSON, and YAML command references from explicitly supplied **sanitized** PowerShell or Bash command dumps.

The skill retains generic parsing, validation, and tests while prohibiting automatic collection or commit of personal command inventories, secrets, private paths, hostnames, or credentials.
